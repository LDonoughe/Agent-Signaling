/**
 * \file  buyer_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def MY_ID
 * \brief Direct access to my_id of buyer agent memory variable. */
#define MY_ID (current_xmachine_buyer->my_id)
/** \def FIRM_ID
 * \brief Direct access to firm_id of buyer agent memory variable. */
#define FIRM_ID (current_xmachine_buyer->firm_id)
/** \def MY_QUAL
 * \brief Direct access to my_qual of buyer agent memory variable. */
#define MY_QUAL (current_xmachine_buyer->my_qual)
